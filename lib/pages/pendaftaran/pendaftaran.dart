import 'package:flutter/material.dart';

class Pendaftaran extends StatelessWidget {
  const Pendaftaran({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('pendaftaran'));
  }
}
